import {CONSTANTS} from "./constants";
export class Row {


  /**
   *
   * @param selector where the component will attach too
   * @param routes
   */
  constructor(public id: number, public fields) {
  }


  /*  addRow(row) {
   this.rows.push(row)
   this.render()
   }

   addRows(rows) {
   this.rows = this.rows.concat(rows)
   this.render()
   }*/
}

